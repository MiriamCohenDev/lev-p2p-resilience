org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler cannot close its own caches under this project's
# build directory on Windows: `:url_launcher_android:compileDebugKotlin` fails
# with "Could not close incremental caches", reproducibly, even after deleting
# the cache and stopping every daemon. Turning incremental compilation off costs
# a slower rebuild of the plugin modules and nothing else — no Dart code is
# affected, and Flutter's own hot reload does not go through Kotlin.
kotlin.incremental=false
